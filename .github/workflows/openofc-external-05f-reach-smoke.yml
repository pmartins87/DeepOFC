name: OpenOFC external 05F reach audit smoke

on:
  push:
    branches:
      - research/external-ofc-solver-audit-20260827
    paths:
      - 'tools/openofc_solver/external_hidden_discard_overlap_reach_audit.py'
      - 'tools/openofc_solver/test_external_hidden_discard_overlap_reach_audit.py'
      - '.github/workflows/openofc-external-05f-reach-smoke.yml'
  workflow_dispatch:

jobs:
  reach-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PYTHONHASHSEED: '0'
      PYTHONDONTWRITEBYTECODE: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install canonical test dependencies
        run: python -m pip install -r requirements-dev.txt
      - name: Validate 05F conditional reach audit
        run: |
          PYTHONPATH=.:tools/openofc_solver \
            python -m pytest -q tools/openofc_solver/test_external_hidden_discard_overlap_reach_audit.py
