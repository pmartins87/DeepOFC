name: OpenOFC M5N F17 runtime calibration

on:
  push:
    branches:
      - strategy/m5h-normal-heldout-evidence
    paths:
      - tools/openofc_solver/run_m5n_normal_fantasy_runtime_calibration.py
      - tools/openofc_solver/run_m5n_normal_fantasy_runtime_calibration_f17.py
      - .github/workflows/openofc-m5n-runtime-calibration-f17.yml
  workflow_dispatch:

jobs:
  calibration:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PYTHONHASHSEED: '0'
      PYTHONDONTWRITEBYTECODE: '1'
      OMP_NUM_THREADS: '1'
      OPENBLAS_NUM_THREADS: '1'
      MKL_NUM_THREADS: '1'
      NUMEXPR_NUM_THREADS: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install canonical test dependencies
        run: python -m pip install -r requirements-dev.txt
      - name: Recheck M5N paired mechanics
        run: |
          PYTHONPATH=tools/openofc_solver \
            python -m pytest -q tools/openofc_solver/test_m5n_normal_fantasy_paired_screening.py
      - name: Run F17 phase-timed M5N calibration
        run: |
          PYTHONPATH=tools/openofc_solver \
            python tools/openofc_solver/run_m5n_normal_fantasy_runtime_calibration_f17.py
      - name: Validate F17 calibration authority firewall
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          data = json.loads(Path('artifacts/m5n_normal_fantasy_runtime_calibration_f17.json').read_text(encoding='utf-8'))
          assert data['authority'] == 'RUNTIME_CALIBRATION_NOT_STRATEGIC_EVIDENCE'
          assert data['source_timeout_run'] == 33089463461
          assert data['state'] == 'B1:P0F17:P1F0'
          assert data['summary']['original_pilot_strategic_result_available'] is False
          assert data['summary']['calibration_is_strategic_evidence'] is False
          assert data['summary']['production_certification_eligible'] is False
          assert data['summary']['real_routes_certified'] == 0
          assert data['timings']['total_measured_seconds'] > 0.0
          print('OPENOFC_M5N_F17_RUNTIME_CALIBRATION_FIREWALL=PASS')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: openofc-m5n-f17-runtime-calibration
          path: artifacts/m5n_normal_fantasy_runtime_calibration_f17.json
          if-no-files-found: error
          retention-days: 14
